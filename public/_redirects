# Static files - must be served directly (order matters - specific rules BEFORE wildcard)

# AdSense verification file (CRITICAL for Google AdSense approval)
/ads.txt        /ads.txt        200!
/app-ads.txt    /ads.txt        200

# SEO files
/sitemap.xml    /sitemap.xml    200!
/robots.txt     /robots.txt     200!

# Monetag service worker - must be served directly from the public root
/sw.js          /sw.js          200!

# SPA fallback - all other routes go to index.html
/*    /index.html   200
